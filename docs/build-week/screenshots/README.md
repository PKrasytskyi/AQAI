# Screenshots

Runtime screenshots and browser snapshots are intentionally not committed because they are transient discovery artifacts. The reproducible demo evidence is captured in the summaries one directory above; a submission video can use the corresponding local `target/ai-run` artifacts.
