schemaVersion: demo-manifest.v2

demo:
  id: orangehrm-login-user-menu-logout-v1

inputs:
  projectProfile: demo/orangehrm-login-logout/project-profile.yaml
  requirementFixture: demo/orangehrm-login-logout/requirements.md
  requiredEnvironment:
    - TEST_VALID_USERNAME
    - TEST_VALID_PASSWORD
    - OPENAI_API_KEY
    - KNOWLEDGE_DB_STATUS

expected:
  pageCapabilities:
    - AUTHENTICATION
    - AUTHENTICATED_AREA
    - USER_MENU
    - LOGOUT
  scenarioIds:
    - REQ-001
    - REQ-002
    - REQ-003
    - REQ-004
  pomNames:
    - LoginPage
    - DashboardPage
  logoutAccessMode: USER_MENU
  lifecycle:
    - AUTHENTICATION
    - AUTHENTICATED_AREA
    - USER_MENU
    - LOGOUT
  finalRoute: /auth/login
  finalState: UNAUTHENTICATED_LOGIN_READY

runtime:
  databaseMode: without-db-baseline
  aiMode: pom-contract-only

schemas:
  requirements: normalized-requirement-bundle.v1
  structuredBehaviorContracts: structured-behavior-contracts.v1
  requirementGovernance: requirement-governance-bundle.v1
  canonicalTestCase: canonical-test-case-bundle.v1
  goldenRequirementSnapshot: golden-requirement-snapshot.v1
  pomContract: pom-contract-v1
  pageEnrichment: page-model-enrichment-record.v1
  qualityReport: ai-run-quality-summary.v1
