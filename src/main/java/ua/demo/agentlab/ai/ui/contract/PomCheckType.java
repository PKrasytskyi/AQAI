package ua.demo.agentlab.ai.ui.contract;

public enum PomCheckType {
    VISIBLE,
    TEXT_CONTAINS,
    TEXT_EQUALS,
    TEXT_PRESENT,
    URL_CONTAINS,
    URL_EQUALS,
    ATTRIBUTE_EQUALS,
    COUNT_GREATER_THAN,
    LIST_TEXTS
}
